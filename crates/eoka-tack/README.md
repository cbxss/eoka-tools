# eoka-tack

Tack adapter for Eoka browser automation tools.

This crate exposes Eoka's protocol catalog as a Tack `ToolSet`, preserving the
same paths, descriptions, schemas, tags, and annotations emitted by
`eoka-protocol`.

